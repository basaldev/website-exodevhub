export = Theme

declare const Theme: {
  colors: {
    primary: string
    bg: string
    grey: {
      dark: string
      default: string
      light: string
      ultraLight: string
    }
  }
  transitions: {
    normal: string
  }
  fontSize: {
    small: string
  }
}
